using VForge.Boards;
using System.Collections;
using System.Collections.Generic;
using System.Numerics;
using UnityEngine;

/// <summary>
/// 
/// - Visual representation either in:
///    - Board(placed piece),
///    - UI list(available piece).
/// - Probably uses different prefabs or the same prefab with different contexts.
///
/// </summary>
public class PieceView : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
