using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class VisualBoardElement_ShopItem : VisualBoardElement
{
    //protected override void OnValueChanged()
    //{
    //    if (Value != null && Value is ShopItem shopItem)
    //    {
    //        RectTransform rectTransform = GetComponent<RectTransform>();

    //        rectTransform.sizeDelta = new Vector2(shopItem.Shape.Width * Board.TileSize, shopItem.Shape.Height * Board.TileSize);

    //        // --

    //        Size = new Vector2Int(shopItem.Shape.Width, shopItem.Shape.Height);
    //    }

    //    base.OnValueChanged();
    //}
}
